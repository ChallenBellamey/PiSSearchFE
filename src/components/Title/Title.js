import React from "react";

import "./Title.css";

const Title = () => {
    return (
        <div className="titleContainer">
            <h1 className="title">PiS Search</h1>
        </div>
    );
};

export default Title;